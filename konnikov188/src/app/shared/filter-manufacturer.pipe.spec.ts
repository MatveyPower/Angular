import { FilterManufacturerPipe } from './filter-manufacturer.pipe';

describe('FilterManufacturerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterManufacturerPipe();
    expect(pipe).toBeTruthy();
  });
});
